flask

# Auto daily post pipeline (scripts/auto-daily-post.py)
anthropic>=0.40.0
pytz
Pillow
# Robust JSON repair — Claude occasionally emits structurally broken JSON
# (mismatched brackets, missing commas) that ast.literal_eval can't handle.
json-repair>=0.30.0
# Direct market data fetch — replaces Claude's training-data hallucination
# with verified Yahoo Finance API values. Indices, stocks, FX, commodities,
# crypto all hard-fetched and injected into the prompt as ground truth.
yfinance>=0.2.40
# Exchange trading calendars — authoritative source for NYSE and XKRX
# (Korea Exchange) market holidays. Used to short-circuit the pipeline
# before any Claude/yfinance call on weekends and market holidays.
# Without this, holiday posts (e.g. New Year, Christmas, Chuseok) get
# generated from training-data hallucination and ruin trust.
exchange-calendars>=4.5.0
