name: Daily Cron Monitor (auto-rescue missing slots)

# 매시간 30분에 발화 → 6개 슬롯 전부 검사 → 발행 시각이 지났는데
# 아직 main에 없는 슬롯은 즉시 rescue 발화.
#
# 이전 버전은 각 슬롯별 정확한 시각 +30분에 1번만 발화했음. 그 발화
# 자체가 GitHub Actions 트래픽으로 누락되면 보충 메커니즘 없음 →
# 5/21 kr-close 누락 사건처럼 1시간+ 지연돼도 자동 복구 불가.
#
# 새 설계: 24시간 매시간 발화. 각 슬롯의 "발행 마감 시각"이 지났는데
# 글이 없으면 발화. 이미 있으면 skip. 한 슬롯의 monitor cron이 누락
# 돼도 다음 시간 monitor가 catch.
#
# 슬롯별 발행 마감 시각 (KST):
# 새 정책 (2026-05-26~): 개장 1시간 전 / 마감 직후 빠르게 발행이 목표.
# deadline 이 지났는데 글 없으면 monitor 가 즉시 rescue 발화.
#   us-close:     06:00 (04:30 cron + 90min 유예 → 마감 +2시간 안)
#   kr-open:      08:00 (07:00 cron + 60min 유예 → 개장 09:00 -1시간 보장)
#   cn-open:      09:30 (08:30 cron + 60min 유예 → 개장 10:30 -1시간 보장)
#   kr-close:     16:30 (15:35 cron + 55min 유예 → 마감 +1시간 안)
#   cn-close:     17:00 (16:05 cron + 55min 유예 → 마감 +1시간 안)
#   us-premarket: 22:30 (21:30 cron + 60min 유예 → 개장 22:30 직전)

on:
  workflow_dispatch:
  # 2026-06-26: 자동발행을 모두 껐으므로 감시용 모니터도 비활성화.
  # 코드는 보존, 필요 시 Actions → Run workflow 로 수동 실행 가능.
  # schedule:
  #   - cron: "30 * * * *"   # 매시간 30분에 발화

permissions:
  contents: read
  actions: write

jobs:
  monitor:
    name: Check all 6 slots, rescue missing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compute today's expected slugs (KST)
        id: dates
        run: |
          publish_date=$(TZ=Asia/Seoul date +%Y-%m-%d)
          trading_date_us_close=$(TZ=Asia/Seoul date -d "yesterday" +%Y-%m-%d)
          hour_kst=$(TZ=Asia/Seoul date +%H)
          dow_kst=$(TZ=Asia/Seoul date +%u)   # 1=Mon..7=Sun
          dow_us_close=$(TZ=Asia/Seoul date -d "yesterday" +%u)
          echo "publish_date=$publish_date" >> "$GITHUB_OUTPUT"
          echo "trading_date_us_close=$trading_date_us_close" >> "$GITHUB_OUTPUT"
          echo "hour_kst=$hour_kst" >> "$GITHUB_OUTPUT"
          echo "dow_kst=$dow_kst" >> "$GITHUB_OUTPUT"
          echo "dow_us_close=$dow_us_close" >> "$GITHUB_OUTPUT"
          echo "now KST: $(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M %Z') (dow=$dow_kst)"
          echo "publish_date=$publish_date, us-close trading_date=$trading_date_us_close (dow=$dow_us_close), hour_kst=$hour_kst"

      - name: Tier-0 weekend short-circuit
        run: |
          # Belt-and-suspenders: even if a downstream check has a date bug,
          # the monitor should NEVER trigger a rescue when today (KST) is
          # Sat/Sun. All 5 slots (kr/cn open + close + us-premarket) compute
          # trading_date = today KST (offset 0), so weekend in KST means
          # weekend trading_date. The 6th slot (us-close, offset -1) uses
          # yesterday — for that we check yesterday's DOW separately below.
          # If today is Sat (6) or Sun (7), bail before any rescue check.
          dow=${{ steps.dates.outputs.dow_kst }}
          if [ "$dow" -ge 6 ]; then
            echo "::notice::🛌 KST today is weekend (dow=$dow). No rescues will fire today (kr/cn slots all reference today's KST date)."
            echo "MONITOR_WEEKEND=1" >> "$GITHUB_ENV"
          fi

      - name: Check & rescue us-close (deadline KST 06:00)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # us-close trading_date = yesterday KST. If yesterday was Sat/Sun,
          # NO recap should exist. Skip rescue. dow values: 1=Mon..7=Sun.
          dow_yest=${{ steps.dates.outputs.dow_us_close }}
          if [ "$dow_yest" -ge 6 ]; then
            echo "::notice::🛌 us-close: yesterday KST is weekend (dow=$dow_yest). No recap expected; skipping rescue."
            exit 0
          fi
          slug="us-tech-recap-${{ steps.dates.outputs.trading_date_us_close }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          if [ "$hour" -lt 6 ] || [ "$hour" -gt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 06) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=us-close

      - name: Check & rescue kr-open (deadline KST 08:00)
        if: env.MONITOR_WEEKEND != '1'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          slug="kr-open-brief-${{ steps.dates.outputs.publish_date }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          if [ "$hour" -lt 8 ] || [ "$hour" -gt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 08) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=kr-open

      - name: Check & rescue cn-open (deadline KST 09:30)
        if: env.MONITOR_WEEKEND != '1'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          slug="cn-open-brief-${{ steps.dates.outputs.publish_date }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          if [ "$hour" -lt 9 ] || [ "$hour" -gt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 09) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=cn-open

      - name: Check & rescue kr-close (deadline KST 16:30)
        if: env.MONITOR_WEEKEND != '1'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          slug="kr-tech-recap-${{ steps.dates.outputs.publish_date }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          if [ "$hour" -lt 16 ] || [ "$hour" -gt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 16) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=kr-close

      - name: Check & rescue cn-close (deadline KST 17:00)
        if: env.MONITOR_WEEKEND != '1'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          slug="cn-tech-recap-${{ steps.dates.outputs.publish_date }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          if [ "$hour" -lt 17 ] || [ "$hour" -gt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 17) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=cn-close

      - name: Check & rescue us-premarket (deadline KST 22:30)
        if: env.MONITOR_WEEKEND != '1'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          slug="us-premarket-${{ steps.dates.outputs.publish_date }}"
          hour=${{ steps.dates.outputs.hour_kst }}
          if [ -d "public/blog/$slug" ]; then
            echo "::notice::✅ $slug exists — skipping"
            exit 0
          fi
          # us-premarket deadline 22:30 — only check at hour >= 23
          if [ "$hour" -lt 23 ]; then
            echo "::notice::⏳ $slug missing but pre-deadline (KST $hour < 23) — skipping"
            exit 0
          fi
          echo "::warning::⚠️ $slug missing past deadline — triggering rescue"
          gh workflow run daily-cron.yml -f slot=us-premarket
